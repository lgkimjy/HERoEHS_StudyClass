#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main(){

    int N, road_length, fuel_price, lowest_fuel;
    queue<int> roads;
    queue<int> fuels;
    int payment, cumualte_roads, flag;
    
    cin >> N;

    for(int i=0; ; i++){
        cin >> road_length;
        roads.push(road_length);
    }

    return 0;
}